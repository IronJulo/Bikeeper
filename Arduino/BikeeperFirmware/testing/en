void en() {
    SMSService smsService(&sim800l);

    char storage[255];
    StringBuffer buffer(storage);
    buffer.clear();

    smsService.read(&buffer);

    if (buffer.equals("Hello")) {
        smsService.send("+336", "World")
    }

}